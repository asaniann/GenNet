# Example secrets - DO NOT COMMIT ACTUAL SECRETS
# Create these using kubectl or external secret management

# Database credentials
# kubectl create secret generic db-credentials \
#   --from-literal=url='postgresql://user:pass@host:5432/gennet' \
#   --from-literal=username='admin' \
#   --from-literal=password='secure-password' \
#   --namespace=gennet-system

# Application secrets
# kubectl create secret generic app-secrets \
#   --from-literal=jwt_secret='your-jwt-secret-key-here' \
#   --from-literal=encryption_key='your-encryption-key' \
#   --namespace=gennet-system

# Neo4j credentials
# kubectl create secret generic neo4j-credentials \
#   --from-literal=user='neo4j' \
#   --from-literal=password='secure-password' \
#   --namespace=gennet-system

# AWS credentials
# kubectl create secret generic aws-credentials \
#   --from-literal=access_key_id='your-access-key' \
#   --from-literal=secret_access_key='your-secret-key' \
#   --namespace=gennet-system

